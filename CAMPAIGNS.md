# 📢 CAMPAIGNS - Документація

## 📊 Загальний огляд

Campaigns — це модуль маркетингових кампаній AGENT CRM, який дозволяє створювати, запускати та відстежувати різні типи маркетингових повідомлень для залучення та утримання клієнтів.

---

## ✅ Реалізовані сценарії (4 з 4)

### 1️⃣ Створення кампанії ✅
**Мета:** Додати нову маркетингову кампанію

**Дії:**
1. Кнопка "+ Кампанія" або "+ Створити кампанію"
2. Заповнення форми:
   - **Назва** (обов'язково)
   - **Опис** (опціонально)
   - **Тип кампанії** (Push / Email / SMS / Банер)
   - **Контент** (обов'язково)
   - **Цільова аудиторія** (Всі / Вибірково)
   - **Розклад** (Дата початку / закінчення)
3. Натиснути "Створити кампанію"

**Результат:** Кампанія зберігається в БД зі статусом "draft"

**Файл:** `app/campaigns/new.tsx`

---

### 2️⃣ Перегляд кампанії ✅
**Мета:** Відображення деталей кампанії

**Дії:**
1. Тап по кампанії в списку → відкриває `/campaigns/[id]`
2. Відображається:
   - Назва та статус
   - Тип кампанії (з іконкою)
   - Опис
   - Контент повідомлення
   - Дати початку/завершення
   - Цільова аудиторія
   - **Статистика:**
     - 📤 Надіслано
     - ✅ Доставлено
     - 👁️ Відкрито
     - 🖱️ Кліки

**Результат:** Користувач бачить повну інформацію про кампанію

**Файл:** `app/campaigns/[id].tsx`

---

### 3️⃣ Запуск push-імітації ✅
**Мета:** Тестування повідомлення перед запуском

**Дії:**
1. На сторінці кампанії натиснути "Надіслати тестове повідомлення"
2. З'являється підтвердження
3. Підтвердити → система показує:
   - **Web:** alert з текстом повідомлення
   - **Mobile:** Alert.alert з повідомленням
4. Оновлюється статистика (sent +1)

**Результат:** Демонструє push-роботу без реального бекенду

**Функція:** `handleTestPush()` в `app/campaigns/[id].tsx`

---

### 4️⃣ Запуск / Завершення / Скасування кампанії ✅
**Мета:** Управління життєвим циклом кампанії

**Дії:**

#### Запуск (Draft → Active)
1. Кнопка "Запустити кампанію"
2. Підтвердження
3. Статус змінюється на "active"
4. Встановлюється start_date

#### Завершення (Active → Completed)
1. Кнопка "Завершити"
2. Підтвердження
3. Статус змінюється на "completed"
4. Встановлюється end_date

#### Скасування (будь-який → Cancelled)
1. Кнопка "Скасувати"
2. Підтвердження
3. Статус змінюється на "cancelled"

**Результат:** Оновлення статусу та відповідних дат

**Функції:** `handleLaunch()`, `handleComplete()`, `handleCancel()`

---

## 🗂️ Структура файлів

```
app/
├── campaigns.tsx              # Список кампаній
├── campaigns/
│   ├── new.tsx                # Створення кампанії
│   └── [id].tsx               # Деталі кампанії

store/
└── campaignsStore.ts          # Store для кампаній

types/
└── index.ts                   # Campaign типи
```

---

## 📊 Типи кампаній

### Push-повідомлення 📣
- **Колір:** Фіолетовий (#8b5cf6)
- **Іконка:** Megaphone
- **Призначення:** Мобільні push-нотифікації

### Email розсилка ✉️
- **Колір:** Синій (#0284c7)
- **Іконка:** Mail
- **Призначення:** Email маркетинг

### SMS розсилка 💬
- **Колір:** Зелений (#16a34a)
- **Іконка:** MessageSquare
- **Призначення:** SMS повідомлення

### Банер 🖼️
- **Колір:** Помаранчевий (#f59e0b)
- **Іконка:** Image
- **Призначення:** Візуальні банери в додатку

---

## 🎨 Статуси кампаній

### Draft (Чернетка)
- **Колір:** Жовтий
- **Іконка:** Clock
- **Дії:** Запустити, Тестувати

### Scheduled (Заплановано)
- **Колір:** Синій
- **Іконка:** Clock
- **Дії:** Скасувати

### Active (Активна)
- **Колір:** Зелений
- **Іконка:** PlayCircle
- **Дії:** Тестувати, Завершити, Скасувати

### Completed (Завершена)
- **Колір:** Сірий
- **Іконка:** CheckCircle
- **Дії:** Тільки перегляд

### Cancelled (Скасовано)
- **Колір:** Червоний
- **Іконка:** XCircle
- **Дії:** Тільки перегляд

---

## 🔄 Життєвий цикл кампанії

```
Draft → Active → Completed
  ↓       ↓
Cancelled ← (з будь-якого статусу)
```

### Переходи статусів:
1. **Створення:** → Draft
2. **Запуск:** Draft → Active
3. **Завершення:** Active → Completed
4. **Скасування:** будь-який → Cancelled
5. **Заплановано:** Draft → Scheduled (автоматично при встановленні дати)

---

## 📱 Список кампаній (`app/campaigns.tsx`)

### Особливості:
- ✅ Пошук за назвою
- ✅ Фільтри за статусом:
  - Всі
  - Активні
  - Заплановані
  - Завершені
- ✅ Pull-to-refresh
- ✅ Empty state
- ✅ Статистика (лічильники)

### Картка кампанії відображає:
- Іконку типу
- Назву
- Тип (Push/Email/SMS/Банер)
- Статус (з кольором)
- Опис (2 рядки)
- Кількість аудиторії
- Кількість переглядів
- Дату початку

### Інтеграція з БД:
```typescript
const { data } = await supabase
  .from('campaigns')
  .select('*')
  .eq('org_id', user.org_id)
  .order('created_at', { ascending: false });
```

---

## ➕ Створення кампанії (`app/campaigns/new.tsx`)

### Форма:
1. **Основна інформація**
   - Назва (обов'язково)
   - Опис

2. **Тип кампанії** (візуальний вибір)
   - 4 картки з іконками
   - Активна картка підсвічується синім

3. **Контент** (залежить від типу)
   - Email → "Текст email"
   - SMS → "Текст SMS" (макс. 160 символів)
   - Banner → "Текст банера"
   - Push → "Текст повідомлення"
   - Лічильник символів

4. **Цільова аудиторія**
   - Всі клієнти (показує кількість)
   - Вибірково (буде доступно після створення)

5. **Розклад**
   - Дата початку (опціонально)
   - Дата завершення (опціонально)
   - Підказка про чернетку

### Валідація:
```typescript
✅ Назва не порожня
✅ Контент не порожній
✅ SMS не більше 160 символів (рекомендація)
```

### Створення:
```typescript
const campaignData = {
  org_id: user.org_id,
  title,
  description,
  type,
  status: 'draft',
  target_audience: { type, count },
  start_date,
  end_date,
  metrics: { sent: 0, delivered: 0, opened: 0, clicked: 0, views: 0 },
  content,
  created_by: user.id,
};
```

---

## 🔍 Деталі кампанії (`app/campaigns/[id].tsx`)

### Розділи:

#### 1. Заголовок
- Іконка типу
- Назва
- Статус (бейдж)
- Кнопка видалення

#### 2. Основна інформація
- Опис
- Дата початку
- Дата завершення
- Цільова аудиторія

#### 3. Контент
- Повний текст повідомлення в окремому блоці

#### 4. Статистика (4 плитки)
- **Надіслано:** синій
- **Доставлено:** зелений
- **Відкрито:** фіолетовий
- **Кліки:** помаранчевий

#### 5. Дії (залежить від статусу)
**Draft:**
- "Запустити кампанію"
- "Надіслати тестове повідомлення"

**Active:**
- "Надіслати тестове повідомлення"
- "Завершити"
- "Скасувати"

**Scheduled:**
- "Скасувати кампанію"

**Completed/Cancelled:**
- Тільки інформаційне повідомлення

---

## 🔔 Тестові повідомлення

### Функція `handleTestPush()`:

```typescript
// 1. Підтвердження
Alert.alert('Тестове повідомлення', 'Надіслати тест?');

// 2. Затримка (імітація відправки)
await new Promise(resolve => setTimeout(resolve, 1000));

// 3. Показ повідомлення
Platform.OS === 'web'
  ? alert(`📱 Тестове повідомлення!\n\n${campaign.title}`)
  : Alert.alert('✅ Відправлено!', 'Тестове повідомлення надіслано');

// 4. Оновлення метрик
const newMetrics = {
  ...metrics,
  sent: (metrics.sent || 0) + 1,
};

await supabase
  .from('campaigns')
  .update({ metrics: newMetrics })
  .eq('id', id);
```

### Переваги:
- ✅ Працює без реального push-сервісу
- ✅ Демонструє функціональність
- ✅ Оновлює статистику
- ✅ Показує різні повідомлення для web/mobile

---

## 🚀 Запуск кампанії

### Функція `handleLaunch()`:

```typescript
await supabase
  .from('campaigns')
  .update({
    status: 'active',
    start_date: new Date().toISOString(),
  })
  .eq('id', id);

updateCampaign(id, { status: 'active', start_date });
Alert.alert('Успіх', 'Кампанію запущено');
```

### Що відбувається:
1. Статус → 'active'
2. Встановлюється start_date
3. Оновлюється UI
4. Показується повідомлення про успіх

---

## ✅ Завершення кампанії

### Функція `handleComplete()`:

```typescript
await supabase
  .from('campaigns')
  .update({
    status: 'completed',
    end_date: new Date().toISOString(),
  })
  .eq('id', id);
```

---

## ❌ Скасування кампанії

### Функція `handleCancel()`:

```typescript
await supabase
  .from('campaigns')
  .update({ status: 'cancelled' })
  .eq('id', id);
```

---

## 🔗 Інтеграція з Dashboard

### AI-підказка про кампанії:
```typescript
const activeCampaigns = campaigns.filter(c => c.status === 'active').length;

if (activeCampaigns === 0 && clients.length > 10) {
  insights.push({
    type: 'recommendation',
    title: 'Створіть маркетингову кампанію',
    message: 'У вас немає активних кампаній. Час залучити нових клієнтів!',
    action: {
      label: 'Створити кампанію',
      route: '/campaigns',
    },
    priority: 'medium',
  });
}
```

### Швидка дія на Dashboard:
```typescript
{
  id: 'new-campaign',
  title: 'Кампанія',
  icon: <Megaphone size={24} color="#f59e0b" />,
  color: '#f59e0b',
  onPress: () => router.push('/campaigns'),
  roles: ['small', 'medium'],
}
```

---

## 📊 Campaigns Store

```typescript
interface CampaignsState {
  campaigns: Campaign[];
  selectedCampaign: Campaign | null;
  isLoading: boolean;

  setCampaigns: (campaigns: Campaign[]) => void;
  addCampaign: (campaign: Campaign) => void;
  updateCampaign: (id: string, updates: Partial<Campaign>) => void;
  deleteCampaign: (id: string) => void;
  selectCampaign: (campaign: Campaign | null) => void;
  setLoading: (loading: boolean) => void;
}
```

### Використання:
```typescript
const { campaigns, addCampaign, updateCampaign } = useCampaignsStore();

// Додати кампанію
addCampaign(newCampaign);

// Оновити статус
updateCampaign(id, { status: 'active' });

// Видалити
deleteCampaign(id);
```

---

## 🗄️ Структура БД

```sql
CREATE TABLE campaigns (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id uuid REFERENCES organizations NOT NULL,
  title text NOT NULL,
  description text,
  type text NOT NULL CHECK (type IN ('push', 'email', 'sms', 'banner')),
  status text NOT NULL DEFAULT 'draft'
    CHECK (status IN ('draft', 'scheduled', 'active', 'paused', 'completed', 'cancelled')),
  target_audience jsonb,
  start_date timestamptz,
  end_date timestamptz,
  metrics jsonb DEFAULT '{"sent":0,"delivered":0,"opened":0,"clicked":0,"views":0}'::jsonb,
  content text,
  created_by uuid REFERENCES users NOT NULL,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);

-- RLS Policies
ALTER TABLE campaigns ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own org campaigns"
  ON campaigns FOR SELECT
  TO authenticated
  USING (org_id IN (SELECT org_id FROM users WHERE id = auth.uid()));

CREATE POLICY "Users can create campaigns"
  ON campaigns FOR INSERT
  TO authenticated
  WITH CHECK (org_id IN (SELECT org_id FROM users WHERE id = auth.uid()));

CREATE POLICY "Users can update own org campaigns"
  ON campaigns FOR UPDATE
  TO authenticated
  USING (org_id IN (SELECT org_id FROM users WHERE id = auth.uid()));

CREATE POLICY "Users can delete own org campaigns"
  ON campaigns FOR DELETE
  TO authenticated
  USING (org_id IN (SELECT org_id FROM users WHERE id = auth.uid()));
```

---

## 🎯 Навігаційні переходи

### До Campaigns:
- Dashboard → Швидка дія "Кампанія"
- Dashboard → AI-підказка → "Створити кампанію"
- Menu (More) → "Кампанії"

### З Campaigns:
- **Список** → `/campaigns`
  - "+ Створити" → `/campaigns/new`
  - Картка кампанії → `/campaigns/[id]`

- **Створення** → `/campaigns/new`
  - "Створити кампанію" → повернення до `/campaigns`

- **Деталі** → `/campaigns/[id]`
  - "Назад" → `/campaigns`
  - "Видалити" → `/campaigns`

---

## ✨ UX особливості

### Візуальні індикатори:
- **Статуси:** кольорові бейджі з іконками
- **Типи:** кольорові іконки (Push/Email/SMS/Банер)
- **Метрики:** кольорові плитки зі статистикою
- **Активний тип:** синя рамка при виборі

### Інтерактивність:
- Pull-to-refresh для оновлення
- Пошук з миттєвою фільтрацією
- Фільтри статусів з лічильниками
- Loading states
- Empty states з CTA кнопками

### Валідація та помилки:
- Перевірка обов'язкових полів
- Alert повідомлення про помилки
- Підтвердження деструктивних дій
- Індикатори завантаження

---

## 📈 Метрики кампаній

### Відстежувані показники:
```typescript
{
  sent: number;        // Надіслано повідомлень
  delivered: number;   // Доставлено
  opened: number;      // Відкрито
  clicked: number;     // Кліків по посиланнях
  views: number;       // Переглядів
}
```

### Оновлення метрик:
```typescript
// При тестовому повідомленні
metrics.sent += 1;

// При реальній кампанії (приклад)
metrics.delivered += successfulDeliveries;
metrics.opened += opens;
metrics.clicked += clicks;
```

---

## 🔐 Безпека

### RLS Policies:
- ✅ Доступ тільки до кампаній своєї організації
- ✅ Перевірка org_id при всіх операціях
- ✅ Authenticated роль для всіх операцій

### Валідація на клієнті:
```typescript
✅ Обов'язкові поля (назва, контент)
✅ Типи даних (enum для type, status)
✅ Дати (end_date > start_date)
✅ Аудиторія (exists клієнти)
```

---

## 📱 Адаптивність

### Мобільні пристрої:
- Сітка 2 колонки для типів кампаній
- Вертикальний scroll
- Touch-friendly кнопки
- Alert повідомлення

### Web:
- Input type="datetime-local" для дат
- Alert() для тестових повідомлень
- Responsive layout

---

## ✅ Checklist готовності

- ✅ Створення кампанії
- ✅ Список кампаній
- ✅ Деталі кампанії
- ✅ Запуск кампанії
- ✅ Завершення кампанії
- ✅ Скасування кампанії
- ✅ Видалення кампанії
- ✅ Тестові повідомлення
- ✅ Пошук та фільтри
- ✅ Статистика
- ✅ Інтеграція з Dashboard
- ✅ Pull-to-refresh
- ✅ Empty states
- ✅ Loading states
- ✅ RLS security
- ✅ TypeScript типи
- ✅ Build успішний

---

## 🎉 Результат

CAMPAIGNS модуль повністю реалізовано:
- **4 з 4 сценаріїв** ✅
- Всі екрани створені
- Повна інтеграція з Dashboard
- Робоча навігація
- Тестування push-повідомлень
- Управління життєвим циклом
- TypeScript ready
- Build успішний

Модуль Campaigns готовий до використання! 🚀
