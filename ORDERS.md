# 📦 ORDERS - Документація

## 📋 Загальний огляд

Orders — це модуль управління замовленнями AGENT CRM, який дозволяє створювати, відстежувати, редагувати та аналізувати замовлення з повною інтеграцією з клієнтами та іншими модулями системи.

---

## ✅ Реалізовані функції

### ✅ Раніше реалізовані:
1. **Список замовлень** (базовий)
2. **Створення замовлення**
3. **Деталі замовлення**

### ✅ Нові функції:
4. **Редагування замовлення** - повна форма редагування (NEW)
5. **Видалення замовлення** - з підтвердженням ✅
6. **Фільтри за статусом** - 6 статусів (NEW)
7. **Фільтри за датою** - сьогодні/тиждень/місяць (NEW)
8. **Пошук** - по назві/опису/клієнту (NEW)
9. **Дублювання на Dashboard** - RecentOrders ✅

---

## 🗂️ Структура файлів

```
app/
├── (tabs)/
│   ├── index.tsx              # Dashboard (RecentOrders ✅)
│   └── orders.tsx             # Список замовлень (ОНОВЛЕНО)
└── orders/
    ├── create.tsx             # Створення (оновлено)
    ├── [id].tsx               # Деталі ✅
    └── edit/
        └── [id].tsx           # Редагування (NEW)

components/
└── RecentOrders.tsx           # Компонент для Dashboard ✅

store/
└── ordersStore.ts             # Store для замовлень

types/
└── index.ts                   # Order типи + OrderStatus
```

---

## 📊 Типи

### Order Interface
```typescript
interface Order {
  id: string;
  org_id: string;
  client_id?: string;
  title: string;
  description?: string;
  status: OrderStatus;
  amount?: number;
  currency?: string;
  assigned_to?: string;
  due_date?: string;
  completed_at?: string;
  created_by: string;
  created_at: string;
  updated_at: string;
}
```

### OrderStatus Type
```typescript
type OrderStatus =
  | 'draft'        // Чернетка
  | 'pending'      // Очікує
  | 'confirmed'    // Підтверджено
  | 'in_progress'  // В роботі
  | 'completed'    // Завершено
  | 'cancelled';   // Скасовано
```

### Status Config
```typescript
const STATUS_CONFIG = {
  draft: {
    label: 'Чернетка',
    color: '#6b7280',
    bg: '#f3f4f6',
    icon: FileText,
  },
  pending: {
    label: 'Очікує',
    color: '#f59e0b',
    bg: '#fef3c7',
    icon: Clock,
  },
  confirmed: {
    label: 'Підтверджено',
    color: '#3b82f6',
    bg: '#dbeafe',
    icon: CheckCircle,
  },
  in_progress: {
    label: 'В роботі',
    color: '#8b5cf6',
    bg: '#ede9fe',
    icon: AlertCircle,
  },
  completed: {
    label: 'Завершено',
    color: '#16a34a',
    bg: '#dcfce7',
    icon: CheckCircle,
  },
  cancelled: {
    label: 'Скасовано',
    color: '#ef4444',
    bg: '#fee2e2',
    icon: XCircle,
  },
};
```

---

## 📱 Список замовлень (`app/(tabs)/orders.tsx`) - ОНОВЛЕНО

### Нові можливості:

#### 1. Пошук (NEW)
```typescript
const filteredOrders = orders.filter((order) => {
  const client = getClient(order.client_id);
  return (
    order.title.toLowerCase().includes(searchQuery.toLowerCase()) ||
    order.description?.toLowerCase().includes(searchQuery.toLowerCase()) ||
    client?.full_name.toLowerCase().includes(searchQuery.toLowerCase())
  );
});
```

**Пошук працює по:**
- ✅ Назві замовлення
- ✅ Опису
- ✅ Імені клієнта

#### 2. Фільтри (NEW)

**Іконка Filter** в header → розгортає панель фільтрів

**Фільтр за статусом:**
```typescript
type FilterType = 'all' | OrderStatus;

// Горизонтальний скрол з кнопками:
- Всі (загальна кількість)
- Чернетка (кількість)
- Очікує (кількість)
- Підтверджено (кількість)
- В роботі (кількість)
- Завершено (кількість)
- Скасовано (кількість)
```

**Фільтр за датою:**
```typescript
type DateFilter = 'all' | 'today' | 'week' | 'month';

const filterByDate = (date: string) => {
  const orderDate = new Date(date);
  const now = new Date();

  switch (dateFilter) {
    case 'today':
      return orderDate.toDateString() === now.toDateString();
    case 'week':
      return orderDate >= new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000);
    case 'month':
      return orderDate >= new Date(now.getTime() - 30 * 24 * 60 * 60 * 1000);
    default:
      return true;
  }
};
```

**Кнопки:**
- Всі
- Сьогодні
- Тиждень
- Місяць

#### 3. Карточка замовлення

**Структура:**
```typescript
✅ Назва (жирним)
✅ Клієнт (якщо є)
✅ Статус (бейдж з іконкою + кольором)
✅ Сума (якщо є)
✅ Термін виконання (якщо є)
✅ ChevronRight
```

**Тап на карточку** → `/orders/[id]`

#### 4. Статистика

**Підрахунок по статусам:**
```typescript
const statusCounts = {
  all: orders.length,
  draft: orders.filter((o) => o.status === 'draft').length,
  pending: orders.filter((o) => o.status === 'pending').length,
  confirmed: orders.filter((o) => o.status === 'confirmed').length,
  in_progress: orders.filter((o) => o.status === 'in_progress').length,
  completed: orders.filter((o) => o.status === 'completed').length,
  cancelled: orders.filter((o) => o.status === 'cancelled').length,
};
```

Показується в кнопках фільтрів.

---

## 🔍 Деталі замовлення (`app/orders/[id].tsx`) - ✅

### Розділи екрану:

#### 1. Заголовок
- Назад
- **Права частина:**
  - Іконка Edit (синя) → `/orders/edit/[id]`
  - Іконка Trash2 (червона) → видалення

#### 2. Основна інформація
- Назва (великим жирним)
- Статус (кольоровий бейдж з іконкою)
- Опис (якщо є)

#### 3. Деталі (кольорові блоки)

**Сума** (зелений блок):
- Іконка DollarSign
- Сума з форматуванням: ₴5,000

**Клієнт** (синій блок, кліковний):
- Іконка User
- Ім'я клієнта
- **Дія:** перехід на `/clients/[id]`

**Термін виконання** (фіолетовий блок):
- Іконка Calendar
- Дата у форматі: "15 листопада 2025"

**Дата завершення** (зелений блок, якщо completed):
- Іконка CheckCircle
- Дата завершення

#### 4. Зміна статусу

**Умова:** якщо НЕ completed і НЕ cancelled

**Карточка "Змінити статус":**
- Список всіх статусів (крім поточного)
- Тап на статус → Alert підтвердження
- Оновлення в БД + локально

```typescript
const handleChangeStatus = async (newStatus: OrderStatus) => {
  const updates: Partial<Order> = {
    status: newStatus,
    updated_at: new Date().toISOString(),
  };

  if (newStatus === 'completed') {
    updates.completed_at = new Date().toISOString();
  }

  await supabase.from('orders').update(updates).eq('id', id);
  setOrder({ ...order, ...updates });
  Alert.alert('Успіх', 'Статус змінено');
};
```

#### 5. Дати створення/оновлення
- Внизу, дрібним текстом

---

## ✏️ Редагування замовлення (`app/orders/edit/[id].tsx`) - NEW

### Форма:

**Поля:**
```typescript
1. ClientSelector (вибір клієнта)
2. Назва * (обов'язкове)
3. Опис (textarea, 4 рядки)
4. Сума (number)
5. Статус (6 кнопок-перемикачів)
6. Термін виконання (дата, YYYY-MM-DD)
```

### Статуси (кнопки-перемикачі):
```typescript
const STATUS_OPTIONS = [
  { value: 'draft', label: 'Чернетка' },
  { value: 'pending', label: 'Очікує' },
  { value: 'confirmed', label: 'Підтверджено' },
  { value: 'in_progress', label: 'В роботі' },
  { value: 'completed', label: 'Завершено' },
  { value: 'cancelled', label: 'Скасовано' },
];
```

**Активна кнопка:** синій фон + білий текст
**Неактивна:** білий фон + сіра рамка

### Валідація:
```typescript
✅ Назва не порожня
⚠️ Інші поля опціональні
```

### Збереження:
```typescript
const updates: Partial<Order> = {
  title: formData.title,
  description: formData.description || undefined,
  amount: formData.amount ? parseFloat(formData.amount) : undefined,
  status: formData.status,
  client_id: formData.clientId || undefined,
  due_date: formData.dueDate
    ? new Date(formData.dueDate).toISOString()
    : undefined,
  updated_at: new Date().toISOString(),
};

// Якщо статус змінено на completed:
if (formData.status === 'completed' && !order?.completed_at) {
  updates.completed_at = new Date().toISOString();
}

await supabase.from('orders').update(updates).eq('id', id);
updateOrder(id, updates);
```

### Процес:
1. Завантаження поточних даних
2. Заповнення форми
3. Редагування полів
4. "Зберегти зміни"
5. Оновлення в БД + Store
6. Alert + повернення назад

---

## 🗑️ Видалення замовлення - ✅

### Підтвердження:
```typescript
Alert.alert(
  'Видалити замовлення',
  `Ви впевнені, що хочете видалити "${order?.title}"?
   Цю дію неможливо скасувати.`,
  [
    { text: 'Скасувати', style: 'cancel' },
    {
      text: 'Видалити',
      style: 'destructive',
      onPress: async () => {
        await supabase.from('orders').delete().eq('id', id);
        deleteOrderFromStore(id);
        Alert.alert('Успіх', 'Замовлення видалено', [
          { text: 'OK', onPress: () => router.back() },
        ]);
      },
    },
  ]
);
```

### Процес:
1. Тап на іконку Trash2
2. Alert з підтвердженням
3. **Скасувати** → нічого
4. **Видалити** →
   - Видалення з БД
   - Видалення зі store
   - Повернення назад
   - Alert "Успіх"

---

## 🏠 Dashboard інтеграція - ✅

### RecentOrders компонент

**Файл:** `components/RecentOrders.tsx`

**Використання на Dashboard:**
```typescript
import RecentOrders from '@/components/RecentOrders';

<RecentOrders orders={orders} limit={5} />
```

**Функціональність:**
- Показує до 5 останніх замовлень
- Назва + дата + сума
- Статус (кольоровий бейдж)
- Тап → `/orders/[id]`
- Кнопка "Переглянути всі" → `/(tabs)/orders`
- Empty state з кнопкою "Створити"
- Loading state

**Карточка замовлення:**
```typescript
✅ Назва (жирним, 1 рядок)
✅ Дата створення + сума
✅ Статус (бейдж з іконкою)
✅ ChevronRight
```

**Статистика показується:**
- Кількість замовлень в кнопці "Переглянути всі"

---

## 🔗 Навігаційні переходи

### До замовлень:
- **Tabs** → Orders (іконка Package)
- **Dashboard** → RecentOrders → тап
- **Dashboard** → QuickActions → "Створити замовлення"
- **Clients** → деталі клієнта → "Створити замовлення"

### З списку замовлень:
- **Тап на картку** → `/orders/[id]`
- **+ (header)** → `/orders/create`
- **Filter (header)** → розгортає фільтри

### З деталей замовлення:
- **Edit (header)** → `/orders/edit/[id]`
- **Trash2 (header)** → видалення + назад
- **Клієнт (блок)** → `/clients/[id]`
- **Змінити статус** → оновлення + reload

### З редагування:
- **Зберегти** → назад до `/orders/[id]`

### З Dashboard:
- **RecentOrders → тап** → `/orders/[id]`
- **"Переглянути всі"** → `/(tabs)/orders`
- **Empty state → "Створити"** → `/orders/create`

---

## 📊 Orders Store

```typescript
interface OrdersState {
  orders: Order[];
  selectedOrder: Order | null;
  isLoading: boolean;

  setOrders: (orders: Order[]) => void;
  addOrder: (order: Order) => void;
  updateOrder: (id: string, updates: Partial<Order>) => void;
  deleteOrder: (id: string) => void;
  selectOrder: (order: Order | null) => void;
  setLoading: (loading: boolean) => void;
}
```

### Використання:
```typescript
const { orders, updateOrder, deleteOrder } = useOrdersStore();

// Оновити
updateOrder(orderId, { status: 'completed' });

// Видалити
deleteOrder(orderId);
```

---

## 🎨 UI/UX особливості

### Кольорова схема статусів:

| Статус       | Колір      | Фон      | Іконка        |
|--------------|------------|----------|---------------|
| Чернетка     | #6b7280    | #f3f4f6  | FileText      |
| Очікує       | #f59e0b    | #fef3c7  | Clock         |
| Підтверджено | #3b82f6    | #dbeafe  | CheckCircle   |
| В роботі     | #8b5cf6    | #ede9fe  | AlertCircle   |
| Завершено    | #16a34a    | #dcfce7  | CheckCircle   |
| Скасовано    | #ef4444    | #fee2e2  | XCircle       |

### Інші кольори:
- **Сума:** Зелений (#16a34a)
- **Клієнт:** Синій (#0284c7)
- **Термін:** Фіолетовий (#8b5cf6)

### Візуальні елементи:
- Кольорові бейджі статусів
- Іконки для всіх дій
- ChevronRight для переходів
- Empty states
- Фільтри (розгортаються)

### Інтерактивність:
- Pull-to-refresh
- Пошук в реальному часі
- Фільтри (клік → перемикання)
- Тап для переходу
- Підтвердження видалення
- Loading states

---

## 🔍 Фільтрація та пошук

### Комбінована фільтрація:
```typescript
const filteredOrders = orders.filter((order) => {
  const client = getClient(order.client_id);

  // Пошук
  const matchesSearch =
    order.title.toLowerCase().includes(searchQuery) ||
    order.description?.toLowerCase().includes(searchQuery) ||
    client?.full_name.toLowerCase().includes(searchQuery);

  // Статус
  const matchesStatus =
    statusFilter === 'all' || order.status === statusFilter;

  // Дата
  const matchesDate = filterByDate(order.created_at);

  return matchesSearch && matchesStatus && matchesDate;
});
```

**Всі фільтри працюють разом!**

### Приклади:
- Пошук "вікна" + статус "В роботі" + дата "Тиждень"
- Клієнт "Іван" + статус "Завершено"
- Сума "5000" + дата "Місяць"

---

## 📱 Адаптивність

### Мобільні пристрої:
- Вертикальний список
- Touch-friendly карточки
- Горизонтальний скрол фільтрів
- Pull-to-refresh
- Native alerts

### Web:
- Responsive layout
- Hover states
- Keyboard navigation (майбутнє)

---

## 🔗 Інтеграції

### З Clients ✅
- ClientSelector в формах
- Відображення клієнта в деталях
- Перехід на профіль клієнта
- Створення замовлення з профілю клієнта

### З Dashboard ✅
- RecentOrders компонент
- Статистика замовлень (KPI)
- QuickActions

### З Meetings (майбутнє)
- Зв'язок замовлень зі зустрічами
- Планування зустрічей по замовленню

---

## ✅ Checklist готовності

### ✅ Базові функції:
- ✅ Список замовлень
- ✅ Створення замовлення
- ✅ Деталі замовлення
- ✅ Редагування замовлення (NEW)
- ✅ Видалення замовлення (NEW)

### ✅ Розширені функції:
- ✅ Пошук замовлень (NEW)
- ✅ Фільтр за статусом (NEW)
- ✅ Фільтр за датою (NEW)
- ✅ Зміна статусу
- ✅ Прив'язка до клієнта
- ✅ Терміни виконання
- ✅ Суми з валютою

### ✅ Інтеграції:
- ✅ Dashboard (RecentOrders)
- ✅ Clients (зв'язок)
- ✅ QuickActions
- ✅ KPI карточки

### ✅ UI/UX:
- ✅ Empty states
- ✅ Loading states
- ✅ Pull-to-refresh
- ✅ Підтвердження видалення
- ✅ Кольорові статуси
- ✅ Іконки дій
- ✅ Бейджі

### ✅ Технічні:
- ✅ TypeScript типи
- ✅ Store інтеграція
- ✅ Supabase CRUD
- ✅ Навігація
- ✅ Build успішний

---

## 🎯 Статистика по статусам

Підрахунок кількості замовлень по статусам для аналітики:

```typescript
const getOrdersStats = () => ({
  total: orders.length,
  draft: orders.filter((o) => o.status === 'draft').length,
  pending: orders.filter((o) => o.status === 'pending').length,
  confirmed: orders.filter((o) => o.status === 'confirmed').length,
  in_progress: orders.filter((o) => o.status === 'in_progress').length,
  completed: orders.filter((o) => o.status === 'completed').length,
  cancelled: orders.filter((o) => o.status === 'cancelled').length,
  totalAmount: orders.reduce((sum, o) => sum + (o.amount || 0), 0),
  completedAmount: orders
    .filter((o) => o.status === 'completed')
    .reduce((sum, o) => sum + (o.amount || 0), 0),
});
```

Використовується:
- В фільтрах (кількість в кнопках)
- На Dashboard (KPI карточки)
- В аналітиці (майбутнє)

---

## 🎉 Результат

ORDERS модуль повністю реалізовано:
- **9 функцій** ✅
- Всі екрани створені/оновлені
- Повна інтеграція з системою
- Фільтри та пошук
- Зв'язок з Clients
- Dashboard інтеграція
- TypeScript ready
- Build успішний

Orders готовий до використання! 🚀
