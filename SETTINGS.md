# ⚙️ SETTINGS - Документація

## 📋 Загальний огляд

Settings — це модуль налаштувань AGENT CRM, який контролює глобальні параметри всієї системи: мову, тему, сповіщення, інтеграції, конфіденційність та інше.

---

## ✅ Реалізовані функції

### ✅ Раніше реалізовані:
1. **Зміна мови** (uk/en) ✅
2. **Базові налаштування**

### ✅ Нові функції:
3. **Зміна теми** - світла/темна/системна (NEW)
4. **Push-сповіщення toggle** - глобальний перемикач (NEW)
5. **Інтеграції** - список підключених сервісів (NEW)
6. **Очистити кеш** - AsyncStorage.clear() (NEW)
7. **Конфіденційність** - налаштування (NEW)
8. **Про додаток** - інформація, контакти (NEW)
9. **Допомога** - FAQ, підтримка (NEW)
10. **Вийти з акаунту** - logout (NEW)

---

## 🗂️ Структура файлів

```
app/
├── settings.tsx              # Головний екран (ОНОВЛЕНО)
└── settings/
    ├── about.tsx             # Про додаток (NEW)
    └── help.tsx              # Допомога (NEW)

store/
├── appStore.ts               # Theme + Language
└── authStore.ts              # User + Logout
```

---

## ⚙️ Головний екран (`app/settings.tsx`) - ОНОВЛЕНО

### Розділи екрану:

#### 1. Загальні

**Мова (синій):**
```typescript
✅ Іконка Globe
✅ Тап → Alert з вибором:
  - Українська
  - English
  - Скасувати
✅ Зміна в i18n та store
✅ Миттєво для всієї системи
```

**Функція:**
```typescript
const handleLanguagePress = () => {
  Alert.alert('Оберіть мову', '', [
    {
      text: 'Українська',
      onPress: () => {
        setLanguage('uk');
        i18n.changeLanguage('uk');
      },
    },
    {
      text: 'English',
      onPress: () => {
        setLanguage('en');
        i18n.changeLanguage('en');
      },
    },
  ]);
};
```

**Тема (фіолетовий):**
```typescript
✅ Іконка динамічна:
  - Sun (світла)
  - Moon (темна)
  - Smartphone (системна)
✅ Тап → Alert з вибором:
  - Світла
  - Темна
  - Системна (майбутнє)
✅ Зміна в store
✅ Застосовується глобально
```

**Функція:**
```typescript
const handleThemePress = () => {
  Alert.alert('Оберіть тему', '', [
    { text: 'Світла', onPress: () => setTheme('light') },
    { text: 'Темна', onPress: () => setTheme('dark') },
    {
      text: 'Системна',
      onPress: () => Alert.alert('Інформація', 'Буде в наступній версії'),
    },
  ]);
};
```

#### 2. Сповіщення

**Push-сповіщення (жовтий):**
```typescript
✅ Switch перемикач
✅ Збереження в БД (users.push_token)
✅ При увімкненні → Alert з підказкою
✅ Лінк "Налаштувати типи" → /profile/notifications
```

**Функція:**
```typescript
const handlePushToggle = async () => {
  const newValue = !pushEnabled;
  setPushEnabled(newValue);

  await supabase
    .from('users')
    .update({
      push_token: newValue ? 'enabled' : null,
      updated_at: new Date().toISOString(),
    })
    .eq('id', user.id);

  Alert.alert(
    'Успіх',
    newValue
      ? 'Push увімкнено. Налаштуйте типи в Профіль → Сповіщення'
      : 'Push вимкнено'
  );
};
```

**Умовний рендер:**
```typescript
{pushEnabled && (
  <TouchableOpacity onPress={() => router.push('/profile/notifications')}>
    <Text>Налаштувати типи сповіщень</Text>
  </TouchableOpacity>
)}
```

#### 3. Інтеграції

**Підключені сервіси (зелений):**
```typescript
✅ Іконка Link
✅ Показує "Google, Telegram"
✅ Тап → Alert (майбутня функція)
```

**Майбутні інтеграції:**
- Google Calendar
- Telegram Bot
- Gmail
- Viber
- WhatsApp
- Email сервіси

#### 4. Конфіденційність

**Конфіденційність (червоний):**
```typescript
✅ Іконка Shield
✅ Тап → Alert (майбутня функція)
```

**Майбутні налаштування:**
- Видимість профілю
- Хто може бачити email/телефон
- Історія активності
- Дані для аналітики

**Очистити кеш (сірий):**
```typescript
✅ Іконка Database
✅ Тап → Alert підтвердження
✅ Очищення AsyncStorage
✅ Loading state (...)
```

**Функція:**
```typescript
const handleClearCache = () => {
  Alert.alert(
    'Очистити кеш',
    'Це видалить тимчасові файли та дані. Продовжити?',
    [
      { text: 'Скасувати', style: 'cancel' },
      {
        text: 'Очистити',
        style: 'destructive',
        onPress: async () => {
          setClearing(true);
          await AsyncStorage.clear();
          Alert.alert('Успіх', 'Кеш очищено');
          setClearing(false);
        },
      },
    ]
  );
};
```

**Що очищується:**
- AsyncStorage (весь)
- Тимчасові дані
- Кеш зображень (майбутнє)
- Локальні копії

#### 5. Інше

**Про додаток (синій):**
```typescript
✅ Іконка Info
✅ Перехід → /settings/about
```

**Допомога та підтримка (фіолетовий):**
```typescript
✅ Іконка HelpCircle
✅ Перехід → /settings/help
```

#### 6. Вийти з акаунту

**Logout (червоний):**
```typescript
✅ Іконка LogOut
✅ Червоний текст
✅ Alert підтвердження
✅ Logout + signOut Supabase
✅ Перехід → /auth/login
```

**Функція:**
```typescript
const handleSignOut = () => {
  Alert.alert('Вийти', 'Ви впевнені?', [
    { text: 'Скасувати', style: 'cancel' },
    {
      text: 'Вийти',
      style: 'destructive',
      onPress: async () => {
        logout();
        await supabase.auth.signOut();
        router.replace('/auth/login');
      },
    },
  ]);
};
```

#### 7. Версія додатку

**Footer:**
```typescript
✅ "AGENT CRM v1.0.0"
✅ "© 2025 AGENT. Всі права захищені."
```

---

## 📱 Про додаток (`app/settings/about.tsx`) - NEW

### Розділи:

#### 1. Заголовок
- Градієнтний логотип (blue → purple)
- Іконка Smartphone
- Назва "AGENT CRM"
- Версія "1.0.0"
- Build "100 (2025)"

#### 2. Про AGENT CRM
**Текст:**
```
AGENT CRM — це комплексна система управління взаємовідносинами з
клієнтами, розроблена спеціально для українського ринку.

Наша місія — надати малому та середньому бізнесу інструменти для
ефективного управління клієнтами, замовленнями та аналітикою.
```

#### 3. Можливості

**4 блоки з іконками:**

**Управління клієнтами (синій):**
- База клієнтів
- Історія взаємодій
- Карта розташування

**Замовлення та зустрічі (зелений):**
- Відстеження замовлень
- Планування зустрічей
- Нагадування

**Аналітика (фіолетовий):**
- Звіти
- Графіки
- KPI
- Прогнозування

**Маркетинг (жовтий):**
- Кампанії
- Розсилки
- Банери
- Промо-матеріали

#### 4. Контакти

**4 лінки (кліковні):**

**Веб-сайт (синій):**
```typescript
Linking.openURL('https://agent-crm.com')
```

**Email (зелений):**
```typescript
Linking.openURL('mailto:support@agent-crm.com')
```

**GitHub (сірий):**
```typescript
Linking.openURL('https://github.com/agent-crm')
```

**Twitter (синій):**
```typescript
Linking.openURL('https://twitter.com/agent_crm')
```

#### 5. Правова інформація

**2 лінки:**
- Умови використання
- Політика конфіденційності

#### 6. Footer
```
© 2025 AGENT CRM. Всі права захищені.
Made with ❤️ in Ukraine
```

---

## 🆘 Допомога (`app/settings/help.tsx`) - NEW

### Розділи:

#### 1. Як ми можемо допомогти?

**3 кнопки зв'язку:**

**Написати в підтримку (синій):**
```typescript
✅ Іконка Mail
✅ support@agent-crm.com
✅ Лінк → mailto:
```

**Telegram підтримка (синій):**
```typescript
✅ Іконка MessageCircle
✅ Пн-Пт 9:00-18:00
✅ Лінк → https://t.me/agent_crm_support
```

**Гаряча лінія (зелений):**
```typescript
✅ Іконка Phone
✅ +38 (044) 234-56-78
✅ Лінк → tel:
```

#### 2. Корисні ресурси

**Документація:**
```typescript
✅ Іконка Book
✅ Детальні інструкції та гайди
✅ Лінк → https://agent-crm.com/docs
```

**Відео-уроки:**
```typescript
✅ Іконка Video
✅ YouTube канал з туторіалами
✅ Лінк → https://youtube.com/@agent-crm
```

#### 3. Часті запитання (FAQ)

**8 питань з розгортанням:**

1. **Як додати нового клієнта?**
   - Клієнти → + → форма → зберегти
   - Або з карти

2. **Як створити замовлення?**
   - Замовлення → + → клієнт → деталі
   - Або з профілю клієнта

3. **Як налаштувати сповіщення?**
   - Профіль → Сповіщення
   - Push + типи

4. **Як запланувати зустріч?**
   - Календар → + → деталі
   - Нагадування

5. **Як змінити мову?**
   - Налаштування → Мова
   - uk/en миттєво

6. **Як працює QR-візитка?**
   - Профіль → QR
   - Показати → сканування

7. **Як змінити тариф?**
   - Профіль → Підписка
   - Обрати → оплата

8. **Як очистити кеш?**
   - Налаштування → Кеш
   - Підтвердити

**Інтерактивність:**
```typescript
const [expandedIndex, setExpandedIndex] = useState<number | null>(null);

const toggleFAQ = (index: number) => {
  setExpandedIndex(expandedIndex === index ? null : index);
};
```

**Іконки:**
- Згорнуто → ChevronDown
- Розгорнуто → ChevronUp

#### 4. Не знайшли відповідь?

**Блок з кнопкою:**
```typescript
✅ Іконка HelpCircle
✅ "Зв'яжіться з підтримкою"
✅ Кнопка "Написати в підтримку"
```

---

## 🔗 Навігаційні переходи

### До налаштувань:
- **Tabs** → More → Settings
- **Profile** → Settings
- **Dashboard** → швидка дія

### З головних налаштувань:
- **Мова** → Alert вибору
- **Тема** → Alert вибору
- **Push** → toggle + /profile/notifications
- **Інтеграції** → Alert (майбутнє)
- **Конфіденційність** → Alert (майбутнє)
- **Кеш** → Alert підтвердження
- **Про додаток** → `/settings/about`
- **Допомога** → `/settings/help`
- **Вийти** → Alert → logout → `/auth/login`

### З "Про додаток":
- **Веб-сайт** → browser
- **Email** → mailto:
- **GitHub** → browser
- **Twitter** → browser
- **Умови** → browser
- **Політика** → browser

### З "Допомога":
- **Email підтримка** → mailto:
- **Telegram** → browser/app
- **Телефон** → tel:
- **Документація** → browser
- **YouTube** → browser/app
- **FAQ** → розгортання
- **Кнопка підтримки** → mailto:

---

## 📊 Stores

### AppStore
```typescript
interface AppState {
  theme: Theme;           // 'light' | 'dark'
  language: Language;     // 'uk' | 'en'
  isOnboarded: boolean;

  setTheme: (theme: Theme) => void;
  setLanguage: (language: Language) => void;
  setOnboarded: (onboarded: boolean) => void;
}
```

**Використання:**
```typescript
const { theme, language, setTheme, setLanguage } = useAppStore();

// Змінити тему
setTheme('dark');

// Змінити мову
setLanguage('en');
i18n.changeLanguage('en');
```

### AuthStore
```typescript
interface AuthState {
  user: User | null;
  isAuthenticated: boolean;
  isLoading: boolean;

  setUser: (user: User | null) => void;
  setLoading: (loading: boolean) => void;
  logout: () => void;
}
```

**Використання:**
```typescript
const { user, logout } = useAuthStore();

// Вийти
logout();
await supabase.auth.signOut();
router.replace('/auth/login');
```

---

## 🎨 UI/UX особливості

### Кольорова схема:

| Секція              | Колір      | Іконка          |
|---------------------|------------|-----------------|
| Мова                | #0284c7    | Globe           |
| Тема                | #8b5cf6    | Sun/Moon        |
| Push                | #f59e0b    | Bell            |
| Інтеграції          | #16a34a    | Link            |
| Конфіденційність    | #ef4444    | Shield          |
| Кеш                 | #6b7280    | Database        |
| Про додаток         | #0284c7    | Info            |
| Допомога            | #8b5cf6    | HelpCircle      |
| Logout              | #ef4444    | LogOut          |

### Візуальні елементи:
- Секції з заголовками (сірий текст)
- Кольорові іконки в кружках
- Switch перемикачі
- ChevronRight для переходів
- Alert діалоги
- External link іконки
- FAQ розгортання

### Інтерактивність:
- Тап на пункти
- Alert вибору/підтвердження
- Switch toggle
- Розгортання FAQ
- Зовнішні лінки (browser/apps)
- Loading states

---

## 🌐 Мова (i18n)

### Підтримувані мови:
```typescript
✅ Українська (uk) - за замовчуванням
✅ English (en)
```

### Зміна мови:
```typescript
// В store
setLanguage('en');

// В i18n
i18n.changeLanguage('en');

// Результат: миттєва зміна всієї системи
```

### Локалізація:
- UI тексти
- Повідомлення
- Помилки
- Підтвердження
- Підказки

---

## 🎨 Тема

### Підтримувані теми:
```typescript
✅ Світла (light) - за замовчуванням
✅ Темна (dark)
⏳ Системна (system) - майбутнє
```

### Зміна теми:
```typescript
setTheme('dark');

// Результат: зміна кольорів всієї системи
```

### Що змінюється:
- Фон екранів
- Кольори текстів
- Кольори карточок
- Бордери
- Іконки (частково)

---

## 💾 Очищення кешу

### AsyncStorage.clear()

**Що очищується:**
```typescript
✅ AsyncStorage - весь
✅ Локальні налаштування
✅ Тимчасові дані
✅ Кеш API (частково)
```

**Що НЕ очищується:**
```typescript
❌ Дані з Supabase
❌ Аватар користувача
❌ Аккаунт
❌ Підписка
```

**Використання:**
```typescript
await AsyncStorage.clear();
```

**Коли потрібно:**
- Звільнити місце
- Вирішити проблеми
- Скинути налаштування
- Видалити тимчасові файли

---

## 🔐 Безпека

### Logout Process:
```typescript
1. Alert підтвердження
2. logout() в store
3. supabase.auth.signOut()
4. Очищення токенів
5. router.replace('/auth/login')
6. Очищення сесії
```

### Збереження даних:
```typescript
✅ Мова → AppStore → AsyncStorage
✅ Тема → AppStore → AsyncStorage
✅ Push token → Supabase DB
✅ User settings → Supabase DB
```

---

## 📱 Адаптивність

### Мобільні:
- Native Alert діалоги
- Native Switch
- Touch feedback
- Pull-to-refresh (де потрібно)

### Web:
- Browser alert
- HTML switch
- Hover states
- Click events

### Platform checks:
```typescript
// Немає в Settings
// Всі функції кросплатформні
```

---

## ✅ Checklist готовності

### ✅ Базові функції:
- ✅ Зміна мови (uk/en)
- ✅ Зміна теми (light/dark)
- ✅ Push toggle
- ✅ Очистити кеш
- ✅ Logout

### ✅ Розширені функції:
- ✅ Інтеграції (показ)
- ✅ Конфіденційність (показ)
- ✅ Про додаток (NEW)
- ✅ Допомога (NEW)
- ✅ FAQ (8 питань)
- ✅ Контакти (4 лінки)
- ✅ Підтримка (3 канали)

### ✅ Інтеграції:
- ✅ AppStore (theme, language)
- ✅ AuthStore (user, logout)
- ✅ i18n (мова)
- ✅ AsyncStorage (кеш)
- ✅ Supabase (logout, push_token)
- ✅ Linking (зовнішні лінки)

### ✅ UI/UX:
- ✅ Секції з заголовками
- ✅ Кольорові іконки
- ✅ Alert діалоги
- ✅ Switch перемикачі
- ✅ Loading states
- ✅ FAQ розгортання
- ✅ Зовнішні лінки

### ✅ Технічні:
- ✅ TypeScript типи
- ✅ Store інтеграція
- ✅ Supabase CRUD
- ✅ Навігація
- ✅ Build успішний

---

## 🎉 Результат

SETTINGS модуль повністю реалізовано:
- **10 функцій** ✅
- Всі екрани створені
- Повна інтеграція з системою
- Глобальні налаштування працюють
- Мова змінюється миттєво
- Тема застосовується
- Push toggle в БД
- Кеш очищається
- Logout працює
- Про додаток + Допомога готові
- TypeScript ready
- Build успішний

Settings готовий до використання! 🚀
