# 👥 CLIENTS - Документація

## 📋 Загальний огляд

Clients — це модуль управління клієнтами AGENT CRM, який дозволяє зберігати контактну інформацію, відстежувати історію взаємодій, створювати замовлення та керувати клієнтською базою.

---

## ✅ Реалізовані функції

### ✅ Раніше реалізовані:
1. **Список клієнтів** з пошуком
2. **Створення клієнта**

### ✅ Нові функції:
3. **Деталі клієнта** - повна інформація та історія
4. **Редагування клієнта** - оновлення даних
5. **Видалення клієнта** - з підтвердженням
6. **Створення замовлення** з профілю клієнта

---

## 🗂️ Структура файлів

```
app/
├── (tabs)/
│   └── clients.tsx            # Список клієнтів
└── clients/
    ├── create.tsx             # Створення клієнта
    ├── [id].tsx               # Деталі клієнта (NEW)
    └── edit/
        └── [id].tsx           # Редагування клієнта (NEW)

app/orders/
└── create.tsx                 # Оновлено: приймає clientId

store/
└── clientsStore.ts            # Store для клієнтів

types/
└── index.ts                   # Client типи
```

---

## 📊 Типи

### Client Interface
```typescript
interface Client {
  id: string;
  org_id: string;
  full_name: string;
  email?: string;
  phone?: string;
  company?: string;
  address?: string;
  latitude?: number;
  longitude?: number;
  notes?: string;
  tags?: string[];
  rating?: number;
  created_by: string;
  created_at: string;
  updated_at: string;
}
```

---

## 📱 Список клієнтів (`app/(tabs)/clients.tsx`)

### Оновлення:

**Було:**
- Empty state
- Кнопка створення

**Стало:**
- ✅ Завантаження з БД
- ✅ Пошук (за ім'ям, компанією, email, телефоном)
- ✅ Pull-to-refresh
- ✅ Карточки клієнтів з інформацією
- ✅ Перехід на деталі (тап на картці)
- ✅ Empty state + Loading state

### Карточка клієнта:
```typescript
✅ Градієнтна аватарка (ініціал)
✅ Повне ім'я (жирним)
✅ Компанія (якщо є)
✅ Телефон з іконкою
✅ Email з іконкою
✅ ChevronRight (індикатор переходу)
```

### Пошук:
```typescript
const filteredClients = clients.filter(
  (client) =>
    client.full_name.toLowerCase().includes(searchQuery.toLowerCase()) ||
    client.company?.toLowerCase().includes(searchQuery.toLowerCase()) ||
    client.email?.toLowerCase().includes(searchQuery.toLowerCase()) ||
    client.phone?.toLowerCase().includes(searchQuery.toLowerCase())
);
```

---

## 🔍 Деталі клієнта (`app/clients/[id].tsx`) - NEW

### Розділи екрану:

#### 1. Заголовок
- Назад
- **Права частина:**
  - Іконка Edit (синя) → редагування
  - Іконка Trash2 (червона) → видалення

#### 2. Профіль клієнта
**Аватарка:**
- Градієнтний круг (blue → purple)
- Ініціал (великий білий текст)

**Інформація:**
- Повне ім'я (великим жирним)
- Компанія (якщо є)
- Рейтинг зірками (якщо є)

#### 3. Контактна інформація (кліковані блоки)

**Телефон** (синій блок):
- Іконка Phone
- Номер телефону
- ChevronRight
- **Дія:** `Linking.openURL('tel:...')`

**Email** (фіолетовий блок):
- Іконка Mail
- Email адреса
- ChevronRight
- **Дія:** `Linking.openURL('mailto:...')`

**Адреса** (зелений блок):
- Іконка MapPin
- Адреса текстом
- ChevronRight (якщо є координати)
- **Дія:** перехід на карту з маркером

#### 4. Нотатки
- Іконка MessageSquare
- Текст нотаток (якщо є)

#### 5. Теги
- Масив тегів
- Блакитні бейджі з округленням

#### 6. Статистика (2 плитки)
- **Замовлень:** кількість замовлень клієнта
- **Зустрічей:** кількість зустрічей

#### 7. Кнопка "Створити замовлення"
- Великав синя кнопка
- Перехід на `/orders/create` з `clientId` та `clientName`

#### 8. Останні замовлення (якщо є)
- До 3 останніх замовлень
- Назва, дата, сума
- Тап → `/orders/[id]`
- "Всі" → список замовлень

#### 9. Останні зустрічі (якщо є)
- До 3 останніх зустрічей
- Назва, дата/час
- Тап → `/meetings/[id]`
- "Всі" → список зустрічей

#### 10. Дата створення
- Внизу, дрібним текстом

---

## ✏️ Редагування клієнта (`app/clients/edit/[id].tsx`) - NEW

### Форма:

**Поля (всі опціональні крім ім'я):**
```typescript
1. Повне ім'я *
2. Телефон
3. Email
4. Компанія
5. Адреса
6. Нотатки (textarea, 4 рядки)
```

### Валідація:
```typescript
✅ Ім'я не порожнє
⚠️ Інші поля опціональні
```

### Збереження:
```typescript
const updates: Partial<Client> = {
  full_name: formData.full_name,
  email: formData.email || undefined,
  phone: formData.phone || undefined,
  company: formData.company || undefined,
  address: formData.address || undefined,
  notes: formData.notes || undefined,
  updated_at: new Date().toISOString(),
};

await supabase
  .from('clients')
  .update(updates)
  .eq('id', id);

updateClient(id, updates);
```

### Процес:
1. Завантаження поточних даних
2. Заповнення форми
3. Редагування
4. "Зберегти зміни"
5. Оновлення в БД + Store
6. Alert + повернення назад

---

## 🗑️ Видалення клієнта - NEW

### Підтвердження:
```typescript
Alert.alert(
  'Видалити клієнта',
  `Ви впевнені, що хочете видалити ${client.full_name}?
   Цю дію неможливо скасувати.`,
  [
    { text: 'Скасувати', style: 'cancel' },
    {
      text: 'Видалити',
      style: 'destructive',
      onPress: async () => {
        await supabase.from('clients').delete().eq('id', id);
        deleteClientFromStore(id);
        Alert.alert('Успіх', 'Клієнта видалено', [
          { text: 'OK', onPress: () => router.back() },
        ]);
      },
    },
  ]
);
```

### Процес:
1. Тап на іконку Trash2
2. Alert з підтвердженням
3. **Скасувати** → нічого
4. **Видалити** →
   - Видалення з БД
   - Видалення зі store
   - Повернення назад
   - Alert "Успіх"

---

## 📦 Створення замовлення з профілю клієнта - NEW

### Оновлення `app/orders/create.tsx`:

**Було:**
```typescript
// Ручний вибір клієнта
```

**Стало:**
```typescript
const params = useLocalSearchParams<{
  clientId?: string;
  clientName?: string;
}>();

// Якщо clientId передано:
✅ Показується підказка "Замовлення для: {clientName}"
✅ ClientSelector disabled (не можна змінити)
✅ ClientSelector pre-filled
```

### Перехід з деталей клієнта:
```typescript
router.push({
  pathname: '/orders/create',
  params: {
    clientId: id,
    clientName: client?.full_name,
  },
});
```

### UI:
- Блакитний блок з текстом "Замовлення для: {ім'я}"
- ClientSelector показує вибраного клієнта
- Неможливо змінити клієнта

---

## 🔗 Навігаційні переходи

### До клієнтів:
- **Tabs** → Clients (іконка Users)
- **Dashboard** → Швидка дія "Клієнти"

### З списку клієнтів:
- **Тап на картку** → `/clients/[id]`
- **+ (header)** → `/clients/create`
- **MapPin (header)** → `/map/clients`

### З деталей клієнта:
- **Edit (header)** → `/clients/edit/[id]`
- **Trash2 (header)** → видалення + назад
- **Створити замовлення** → `/orders/create?clientId=...`
- **Замовлення** → `/orders/[id]`
- **Зустрічі** → `/meetings/[id]`
- **Адреса з координатами** → `/map/picker`

### З редагування:
- **Зберегти** → назад до `/clients/[id]`

---

## 📊 Clients Store

```typescript
interface ClientsState {
  clients: Client[];
  selectedClient: Client | null;
  isLoading: boolean;

  setClients: (clients: Client[]) => void;
  addClient: (client: Client) => void;
  updateClient: (id: string, updates: Partial<Client>) => void;
  deleteClient: (id: string) => void;
  selectClient: (client: Client | null) => void;
  setLoading: (loading: boolean) => void;
}
```

### Використання:
```typescript
const { clients, updateClient, deleteClient } = useClientsStore();

// Оновити
updateClient(clientId, { full_name: 'Нове ім\'я' });

// Видалити
deleteClient(clientId);
```

---

## 🎨 UI/UX особливості

### Кольорова схема:
- **Аватарки:** Градієнт blue → purple
- **Телефон:** Синій (#0284c7)
- **Email:** Фіолетовий (#8b5cf6)
- **Адреса:** Зелений (#16a34a)
- **Статистика:** Синій + фіолетовий

### Візуальні елементи:
- Градієнтні аватарки
- Кліковані контактні блоки
- Іконки для всіх дій
- ChevronRight для переходів
- Empty states

### Інтерактивність:
- Pull-to-refresh
- Пошук в реальному часі
- Тап для переходу
- Підтвердження видалення
- Loading states

---

## 📱 Linking (зовнішні дії)

### Дзвінок:
```typescript
const handleCall = () => {
  if (client?.phone) {
    Linking.openURL(`tel:${client.phone}`);
  }
};
```

### Email:
```typescript
const handleEmail = () => {
  if (client?.email) {
    Linking.openURL(`mailto:${client.email}`);
  }
};
```

### Карта:
```typescript
const handleShowOnMap = () => {
  if (client?.latitude && client?.longitude) {
    router.push({
      pathname: '/map/picker',
      params: {
        latitude: client.latitude,
        longitude: client.longitude,
        title: client.full_name,
      },
    });
  }
};
```

---

## 📱 Адаптивність

### Мобільні пристрої:
- Вертикальний список
- Touch-friendly карточки
- Native linking
- Pull-to-refresh

### Web:
- Responsive layout
- mailto: та tel: лінки працюють
- Hover states

---

## ✅ Checklist готовності

### ✅ Базові функції:
- ✅ Список клієнтів
- ✅ Створення клієнта
- ✅ Деталі клієнта (NEW)
- ✅ Редагування клієнта (NEW)
- ✅ Видалення клієнта (NEW)

### ✅ Розширені функції:
- ✅ Пошук клієнтів
- ✅ Статистика (замовлення/зустрічі)
- ✅ Створення замовлення з профілю (NEW)
- ✅ Історія замовлень
- ✅ Історія зустрічей

### ✅ Інтеграції:
- ✅ Linking (tel:/mailto:)
- ✅ Карта (координати)
- ✅ Замовлення (зв'язок)
- ✅ Зустрічі (зв'язок)

### ✅ UI/UX:
- ✅ Empty states
- ✅ Loading states
- ✅ Pull-to-refresh
- ✅ Підтвердження видалення
- ✅ Градієнтні аватарки
- ✅ Кліковані контакти

### ✅ Технічні:
- ✅ TypeScript типи
- ✅ Store інтеграція
- ✅ Supabase CRUD
- ✅ Навігація
- ✅ Build успішний

---

## 🎉 Результат

CLIENTS модуль повністю реалізовано:
- **6 функцій** ✅
- Всі екрани створені
- Повна інтеграція з системою
- Linking працює
- Зв'язок з Orders/Meetings
- TypeScript ready
- Build успішний

Clients готовий до використання! 🚀
