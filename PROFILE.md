# 👤 PROFILE - Документація

## 📋 Загальний огляд

Profile — це модуль управління профілем користувача AGENT CRM, який включає перегляд/редагування даних, завантаження аватара, QR-візитку, управління підпиською, налаштування сповіщень та вибір ролі користувача.

---

## ✅ Реалізовані функції

### ✅ Раніше реалізовані:
1. **Перегляд профілю** (базовий)
2. **Базове редагування**

### ✅ Нові функції:
3. **Завантаження фото** - з галереї/камери (NEW)
4. **QR-візитка** - vCard з контактами (NEW)
5. **Підписка і тариф** - перегляд і порівняння тарифів (NEW)
6. **Рейтинг користувача** - відображення ✅
7. **Вибір ролі** - 5 ролей з різними можливостями (NEW)
8. **Push-сповіщення** - повне налаштування (NEW)

---

## 🗂️ Структура файлів

```
app/
├── profile.tsx                # Головний екран профілю (ОНОВЛЕНО)
└── profile/
    ├── edit.tsx               # Редагування профілю (NEW)
    ├── qr.tsx                 # QR-візитка (NEW)
    ├── subscription.tsx       # Підписка і тариф (NEW)
    └── notifications.tsx      # Налаштування сповіщень (NEW)

types/
└── index.ts                   # User + UserRole типи

store/
└── authStore.ts               # Store користувача
```

---

## 📊 Типи

### User Interface
```typescript
interface User {
  id: string;
  email: string;
  phone?: string;
  full_name: string;
  role: UserRole;
  avatar_url?: string;
  rating?: number;
  org_id?: string;
  org_role?: OrgRole;
  created_at: string;
  updated_at: string;
}
```

### UserRole Type
```typescript
type UserRole =
  | 'citizen'     // Користувач
  | 'individual'  // ІП
  | 'small'       // Малий бізнес
  | 'medium'      // Середній бізнес
  | 'admin';      // Адміністратор
```

### Role Configuration
```typescript
const ROLE_CONFIG = {
  citizen: {
    label: 'Користувач',
    color: '#6b7280',
    icon: User,
    features: ['Базовий функціонал', 'Замовлення', 'Каталог'],
  },
  individual: {
    label: 'Індивідуальний підприємець',
    color: '#3b82f6',
    icon: User,
    features: ['Клієнти', 'Замовлення', 'Зустрічі', 'Базова аналітика'],
  },
  small: {
    label: 'Малий бізнес',
    color: '#8b5cf6',
    icon: Award,
    features: ['Все з Individual', 'Команда 10 осіб', 'Кампанії', 'Аналітика'],
  },
  medium: {
    label: 'Середній бізнес',
    color: '#f59e0b',
    icon: Crown,
    features: ['Все з Small', 'Необмежена команда', 'API', 'Менеджер'],
  },
  admin: {
    label: 'Адміністратор',
    color: '#ef4444',
    icon: Shield,
    features: ['Повний доступ', 'Управління системою'],
  },
};
```

---

## 📱 Головний екран профілю (`app/profile.tsx`) - ОНОВЛЕНО

### Розділи екрану:

#### 1. Заголовок
- Назва "Профіль"
- Назад
- **Права частина:** іконка Edit → `/profile/edit`

#### 2. Аватар та основна інформація
**Аватар (кліковний):**
```typescript
✅ Якщо є avatar_url → Image
✅ Якщо немає → градієнтний круг з ініціалом
✅ Іконка Camera в правому нижньому куті
✅ Тап → Alert з вибором:
  - Вибрати з галереї
  - Зробити фото
  - Скасувати
```

**Завантаження аватара:**
```typescript
1. ImagePicker.requestMediaLibraryPermissionsAsync()
2. ImagePicker.launchImageLibraryAsync() або launchCameraAsync()
3. Завантаження в Supabase Storage (bucket: avatars)
4. Отримання publicUrl
5. Оновлення в БД (users.avatar_url)
6. Оновлення в store
7. Alert "Успіх"
```

**Інформація:**
- Повне ім'я (великим жирним)
- Роль (кольоровий бейдж з іконкою)
- Рейтинг (зірка + число + "рейтинг")

#### 3. Контактна інформація
**Email** (синій блок):
- Іконка Mail
- Email адреса

**Телефон** (зелений блок, якщо є):
- Іконка Phone
- Номер телефону

#### 4. Доступні можливості
Список features з ролі користувача:
- Кружечок (bullet point)
- Назва можливості
- Показується тільки якщо є features

#### 5. Дії
**3 кнопки-пункти:**

**QR-візитка** (фіолетовий):
- Іконка QrCode
- Перехід → `/profile/qr`

**Підписка і тариф** (жовтий):
- Іконка CreditCard
- Перехід → `/profile/subscription`

**Сповіщення** (синій):
- Іконка Bell
- Перехід → `/profile/notifications`

#### 6. Статистика
- Дата реєстрації
- Останнє оновлення

---

## ✏️ Редагування профілю (`app/profile/edit.tsx`) - NEW

### Форма:

**Особиста інформація:**
```typescript
1. Повне ім'я * (обов'язкове)
2. Email (read-only, не можна змінити)
3. Телефон (опціонально)
```

### Вибір ролі (NEW)

**5 ролей на вибір:**

Кожна роль - це карточка з:
- Іконкою (кольорова, в кружечку)
- Назвою
- Описом можливостей
- Галочкою (якщо активна)

**Карточка активної ролі:**
- Рамка кольору ролі
- Фон світлий кольору ролі
- Галочка в кружечку

**При зміні ролі:**
```typescript
// Якщо обрано small або medium:
Alert.alert(
  'Зміна тарифу',
  'Для переходу потрібна активна підписка. Перейти?',
  [
    { text: 'Скасувати' },
    { text: 'Перейти', onPress: () => router.push('/profile/subscription') },
  ]
);

// Інші ролі змінюються одразу
```

### Валідація:
```typescript
✅ Ім'я не порожнє
⚠️ Email незмінний
⚠️ Телефон опціональний
```

### Збереження:
```typescript
const updates = {
  full_name: formData.full_name,
  phone: formData.phone || undefined,
  role: formData.role,
  updated_at: new Date().toISOString(),
};

await supabase.from('users').update(updates).eq('id', user.id);
setUser({ ...user, ...updates });
Alert.alert('Успіх', 'Профіль оновлено');
router.back();
```

### Інформаційний блок:
```typescript
✅ Синій блок з інформацією про зміну ролі
✅ Попередження про необхідність підписки
```

---

## 📱 QR-візитка (`app/profile/qr.tsx`) - NEW

### Функціональність:

**QR-код:**
```typescript
import QRCode from 'react-native-qrcode-svg';

const vCardData = `BEGIN:VCARD
VERSION:3.0
FN:${user.full_name}
EMAIL:${user.email}
TEL:${user.phone}
TITLE:${user.role}
END:VCARD`;

<QRCode value={vCardData} size={200} />
```

**Розділи:**

#### 1. QR-код
- Ім'я користувача (заголовок)
- Підзаголовок "Поділіться контактами"
- QR-код в білому квадраті з тінню
- Підказка "Скануйте для збереження"

#### 2. Кнопки
**2 кнопки поруч:**
- "Поділитися" → Share API
- "Завантажити" → Alert (майбутня функція)

**Share:**
```typescript
await Share.share({
  message: `Візитка ${user.full_name}\n\nEmail: ${user.email}\nТелефон: ${user.phone}`,
  title: 'Моя візитка',
});
```

#### 3. Контактна інформація
Список з іконками:
- Email (синій)
- Телефон (зелений)
- Роль (фіолетовий)

#### 4. Інструкція
**3 кроки:**
1. Покажіть QR-код
2. Сканують камерою
3. Контакти зберігаються

---

## 💳 Підписка і тариф (`app/profile/subscription.tsx`) - NEW

### Поточний тариф

**Карточка вгорі:**
- Назва тарифу (бейдж з кольором)
- Ціна великим шрифтом
- Дата наступного списання (для платних)
- "Безкоштовний тариф" (для citizen)

### Порівняння тарифів

**4 тарифи:**

#### 1. Базовий (citizen)
```typescript
{
  price: 0,
  color: '#6b7280',
  icon: Users,
  features: [
    ✅ Перегляд каталогу
    ✅ Створення замовлень
    ✅ Базова підтримка
    ❌ Клієнти
    ❌ Аналітика
    ❌ Кампанії
  ],
}
```

#### 2. Індивідуальний (individual) - Рекомендуємо
```typescript
{
  price: 199,
  color: '#3b82f6',
  icon: Award,
  features: [
    ✅ Все з Базового
    ✅ Клієнти
    ✅ Замовлення і зустрічі
    ✅ Базова аналітика
    ❌ Команда
    ❌ Кампанії
  ],
  recommended: true, // Бейдж "Рекомендуємо"
}
```

#### 3. Малий бізнес (small)
```typescript
{
  price: 499,
  color: '#8b5cf6',
  icon: Megaphone,
  features: [
    ✅ Все з Індивідуального
    ✅ Команда до 10 осіб
    ✅ Кампанії
    ✅ Розширена аналітика
    ✅ Пріоритетна підтримка
    ❌ API доступ
  ],
}
```

#### 4. Середній бізнес (medium)
```typescript
{
  price: 999,
  color: '#f59e0b',
  icon: Crown,
  features: [
    ✅ Все з Малого бізнесу
    ✅ Необмежена команда
    ✅ API доступ
    ✅ Білий лейбл
    ✅ Персональний менеджер
    ✅ SLA гарантії
  ],
}
```

### Карточка тарифу:

**Структура:**
- Іконка (в кольоровому кружку)
- Назва + ціна
- Список features (✅/❌)
- Кнопка "Обрати тариф"

**Кнопка:**
- Поточний тариф → "Поточний тариф" (outline, disabled)
- Безкоштовний → "Безкоштовно"
- Інші → "Обрати тариф" (синя)

**При натисканні:**
```typescript
Alert.alert(
  'Оновлення тарифу',
  'Інтеграція з платіжною системою буде доступна в наступній версії'
);
```

### Часті запитання

**3 блоки:**
1. Чи можна змінити тариф?
2. Що станеться з даними?
3. Чи є пробний період?

---

## 🔔 Сповіщення (`app/profile/notifications.tsx`) - NEW

### Push-сповіщення

**Головний перемикач:**
```typescript
const [pushEnabled, setPushEnabled] = useState(false);

// Перевірка дозволів
const { status } = await Notifications.getPermissionsAsync();
setPushEnabled(status === 'granted');

// Запит дозволу
const { status } = await Notifications.requestPermissionsAsync();

// Отримання токену
const token = (await Notifications.getExpoPushTokenAsync()).data;

// Збереження в БД
await supabase.from('users').update({
  push_token: token,
}).eq('id', user.id);
```

**Тестове сповіщення:**
```typescript
await Notifications.scheduleNotificationAsync({
  content: {
    title: 'Тестове сповіщення 📬',
    body: 'Це тестове повідомлення від AGENT CRM',
    data: { type: 'test' },
  },
  trigger: {
    seconds: 1,
    type: Notifications.SchedulableTriggerInputTypes.TIME_INTERVAL,
  },
});
```

### Типи сповіщень

**6 категорій з перемикачами:**

#### 1. Замовлення (синій)
- Іконка ShoppingBag
- Нові замовлення та зміни статусу
- За замовчуванням: ✅

#### 2. Клієнти (фіолетовий)
- Іконка Users
- Нові клієнти та повідомлення
- За замовчуванням: ✅

#### 3. Зустрічі (зелений)
- Іконка Calendar
- Нагадування про зустрічі
- За замовчуванням: ✅

#### 4. Кампанії (жовтий)
- Іконка Megaphone
- Статистика та результати
- За замовчуванням: ✅

#### 5. Повідомлення (синій)
- Іконка MessageSquare
- Нові повідомлення від команди
- За замовчуванням: ❌

#### 6. Email сповіщення (сірий)
- Іконка Mail
- Отримувати на email
- За замовчуванням: ✅

### Збереження налаштувань:

```typescript
const saveSettings = async (settings: NotificationSetting[]) => {
  const notificationsData = settings.reduce(
    (acc, setting) => ({
      ...acc,
      [setting.id]: setting.enabled,
    }),
    {}
  );

  await supabase.from('user_settings').upsert({
    user_id: user.id,
    notifications: notificationsData,
    updated_at: new Date().toISOString(),
  });
};
```

### Інформаційний блок:
```typescript
✅ Іконка Bell
✅ "Про сповіщення"
✅ Пояснення що важливі системні завжди приходять
```

---

## 🎨 UI/UX особливості

### Кольорова схема ролей:

| Роль        | Колір    | Іконка  |
|-------------|----------|---------|
| Користувач  | #6b7280  | User    |
| ІП          | #3b82f6  | User    |
| Малий       | #8b5cf6  | Award   |
| Середній    | #f59e0b  | Crown   |
| Адмін       | #ef4444  | Shield  |

### Кольори дій:
- **QR-візитка:** Фіолетовий (#8b5cf6)
- **Підписка:** Жовтий (#f59e0b)
- **Сповіщення:** Синій (#0284c7)

### Візуальні елементи:
- Градієнтний аватар (blue → purple)
- Кольорові бейджі ролей
- Іконки для всіх дій
- Switch перемикачі
- Карточки тарифів
- QR-код з тінню

### Інтерактивність:
- Тап на аватар → вибір фото
- Camera badge на аватарі
- Перемикачі (Switch)
- Кліковані пункти меню
- Alert підтвердження
- Share functionality

---

## 🔗 Навігаційні переходи

### До профілю:
- **Tabs** → More → Profile
- **Dashboard** → Швидка дія
- **Settings** → Profile

### З головного профілю:
- **Edit (header)** → `/profile/edit`
- **Аватар (тап)** → Alert вибору фото
- **QR-візитка** → `/profile/qr`
- **Підписка** → `/profile/subscription`
- **Сповіщення** → `/profile/notifications`

### З редагування:
- **Роль small/medium** → Alert → `/profile/subscription`
- **Зберегти** → назад

### З QR:
- **Поділитися** → Share API
- **Завантажити** → Alert (майбутнє)

### З підписки:
- **Обрати тариф** → Alert (майбутнє)

---

## 📊 Profile Store (AuthStore)

```typescript
interface AuthState {
  user: User | null;
  session: Session | null;
  isLoading: boolean;

  setUser: (user: User) => void;
  setSession: (session: Session | null) => void;
  signOut: () => Promise<void>;
}
```

### Використання:
```typescript
const { user, setUser } = useAuthStore();

// Оновити користувача
setUser({ ...user, avatar_url: newUrl });
```

---

## 🔐 Завантаження фото

### ImagePicker

**Дозволи:**
```typescript
// Галерея
const { status } = await ImagePicker.requestMediaLibraryPermissionsAsync();

// Камера
const { status } = await ImagePicker.requestCameraPermissionsAsync();
```

**Вибір з галереї:**
```typescript
const result = await ImagePicker.launchImageLibraryAsync({
  mediaTypes: ImagePicker.MediaTypeOptions.Images,
  allowsEditing: true,
  aspect: [1, 1],
  quality: 0.8,
});
```

**Зробити фото:**
```typescript
const result = await ImagePicker.launchCameraAsync({
  allowsEditing: true,
  aspect: [1, 1],
  quality: 0.8,
});
```

### Supabase Storage

**Завантаження:**
```typescript
const response = await fetch(uri);
const blob = await response.blob();
const fileName = `${user.id}-${Date.now()}.jpg`;
const filePath = `avatars/${fileName}`;

await supabase.storage.from('avatars').upload(filePath, blob);

const { data: { publicUrl } } = supabase.storage
  .from('avatars')
  .getPublicUrl(filePath);
```

**Bucket configuration:**
- Назва: `avatars`
- Public: true
- File size limit: 5MB
- Allowed types: image/jpeg, image/png

---

## 🔔 Push Notifications

### Expo Notifications

**Налаштування handler:**
```typescript
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: true,
    shouldSetBadge: true,
    shouldShowBanner: true,
    shouldShowList: true,
  }),
});
```

**Отримання токену:**
```typescript
const token = (await Notifications.getExpoPushTokenAsync()).data;
```

**Відправка тестового:**
```typescript
await Notifications.scheduleNotificationAsync({
  content: {
    title: 'Title',
    body: 'Body',
    data: { type: 'test' },
  },
  trigger: {
    seconds: 1,
    type: Notifications.SchedulableTriggerInputTypes.TIME_INTERVAL,
  },
});
```

### Збереження налаштувань

**Таблиця:** `user_settings`

**Структура:**
```sql
CREATE TABLE user_settings (
  user_id UUID PRIMARY KEY REFERENCES users(id),
  notifications JSONB,
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**Приклад даних:**
```json
{
  "orders": true,
  "clients": true,
  "meetings": true,
  "campaigns": true,
  "messages": false,
  "email": true
}
```

---

## 📱 Адаптивність

### Мобільні пристрої:
- Native ImagePicker
- Native Camera
- Native Notifications
- Native Share

### Web:
- ImagePicker (тільки галерея)
- Camera недоступна
- Notifications недоступні
- Share API (якщо підтримується)

### Platform checks:
```typescript
if (Platform.OS === 'web') {
  Alert.alert('Недоступно', 'Функція не підтримується на веб');
  return;
}
```

---

## ✅ Checklist готовності

### ✅ Базові функції:
- ✅ Перегляд профілю
- ✅ Редагування профілю
- ✅ Завантаження аватара (NEW)
- ✅ Відображення рейтингу

### ✅ Розширені функції:
- ✅ QR-візитка (NEW)
- ✅ Підписка і тарифи (NEW)
- ✅ Вибір ролі (NEW)
- ✅ Push-сповіщення (NEW)
- ✅ Типи сповіщень (NEW)

### ✅ Інтеграції:
- ✅ Supabase Storage (аватари)
- ✅ Supabase DB (профіль)
- ✅ ImagePicker (галерея/камера)
- ✅ Expo Notifications
- ✅ QR Code generation
- ✅ Share API

### ✅ UI/UX:
- ✅ Кольорові ролі
- ✅ Бейджі та іконки
- ✅ Switch перемикачі
- ✅ Alert підтвердження
- ✅ Loading states
- ✅ Platform checks

### ✅ Технічні:
- ✅ TypeScript типи
- ✅ Store інтеграція
- ✅ Supabase CRUD
- ✅ Навігація
- ✅ Build успішний

---

## 🎉 Результат

PROFILE модуль повністю реалізовано:
- **8 функцій** ✅
- Всі екрани створені
- Повна інтеграція з системою
- Завантаження фото працює
- QR-візитка готова
- Підписка показує тарифи
- Сповіщення налаштовуються
- Ролі з ієрархією
- TypeScript ready
- Build успішний

Profile готовий до використання! 🚀
